import { PageShell } from "@/components/page-shell";

export default function PlaceOrderPage() {
  return (
    <PageShell title="Place order">
      <p>
        Skeleton: form to add a new order and line items in a single database
        transaction.
      </p>
    </PageShell>
  );
}
